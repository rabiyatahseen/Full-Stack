<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Color References in CSS</title>
    <style>
        /* Color reference using color name */
        .color-name {
            background-color: red; /* 'red' is a named color */
            color: white;
        }
        /* Color reference using HEX code */
        .color-hex {
            background-color: #3498db; /* HEX code for a shade of blue */
            color: white;
        }
        /* Color reference using RGB */
        .color-rgb {
            background-color: rgb(255, 99, 71); /* RGB for tomato red */
            color: white;
        }
        /* Color reference using RGBA (RGB + alpha for opacity) */
        .color-rgba {
            background-color: rgba(75, 0, 130, 0.5); /* RGBA for indigo with 50% opacity */
            color: white;
        }
        /* Color reference using HSL */
        .color-hsl {
            background-color: hsl(120, 100%, 25%); /* HSL for green */
            color: white;
        }
        /* Color reference using HSLA (HSL + alpha for opacity) */
        .color-hsla {
            background-color: hsla(210, 100%, 50%, 0.7); /* HSLA for blue with 70% opacity */
            color: white;
        }
        /* Display styling */
        .container {
            display: flex;
            justify-content: space-around;
            align-items: center;
            flex-direction: column;
        }

        .box {
            width: 900px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 10px;
            font-weight: bold;
            text-align: center;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="box color-name">Color Name: Red</div>
    <div class="box color-hex">HEX Value: #1E90FF</div>
    <div class="box color-rgb">RGB Value: rgb(34, 139, 34)</div>
    <div class="box color-rgba">RGBA Value: rgba(255, 99, 71, 0.7)</div>
    <div class="box color-hsl">HSL Value: hsl(240, 100%, 50%)</div>
    <div class="box color-hsla">HSLA Value: hsla(120, 60%, 70%, 0.6)</div>
</div>

</body>
</html>
